# --- 보안 ---
# 서비스 계정 키, API 키 등 모든 .json 파일
*.json

# --- 로그 및 결과 ---
# 로그 파일
*.log
# 엑셀 결과 파일
*.xlsx
*.xls

# --- Python ---
# 캐시 폴더
__pycache__/
*.pyc

# 가상환경 폴더
venv/
.venv/
env/

# --- IDE ---
# PyCharm / VSCode
.idea/
.vscode/


**2단계: 깃허브의 최신 상태로 커밋 되돌리기 (코드는 유지)**

로컬에 쌓인 모든 커밋(나쁜 커밋, 좋은 커밋 모두)을 취소하고, 모든 코드 변경 사항을 '커밋 대기(staged)' 상태로 되돌립니다. `05d21e3`는 깃허브에 있던 마지막 커밋 ID입니다.

```bash
git reset --soft 05d21e3